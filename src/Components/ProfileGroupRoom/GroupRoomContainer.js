import React from "react";
import { useQuery } from "react-apollo-hooks";
import GroupRoomPresenter from "./GroupRoomPresenter";
import { ALL_GROUPROOM } from "./GroupRoomQueries";

export default () => {
  // const { data, loading, refetch } = useQuery(ALL_GROUPROOM);
  return "hi";

  //    return (
  //         <GroupRoomPresenter
  //             data={data}
  //             loading={loading}
  //         />
  //    );
};
